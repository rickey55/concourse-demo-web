platform: linux

image_resource:
  type: docker-image
  source:
    repository: https://hub.docker.com/rickeysu/jdk8maven
    tag: "tagname"
    insecure_registries: ["https://hub.docker.com/"]
inputs:
- name: source-code

outputs:
- name: target-web

run:
  path: source-code/concourse/tasks/package-web.sh
